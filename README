1 Configuration file tpcds.config

GPLOAD_HOST=192.168.115.130,192.168.115.131,192.168.115.132 # Load data server host,comma separated, multi host for distributed data to multiple nodes.
GP_DBHOST=192.168.115.130                                   # HAWQ server host.
BASH_PROFILE=/usr/local/hawq/greenplum_path.sh              # HAWQ home greenplum_path.sh
DIRS=/data1/hawq/data,/data2/hawq/data                      # Load data server directory ,comma separated, multi directory for distributed data.
DDLTYPE=p                                                   # Table type : o(orc) p(parquet) m(magmaap)
DATASIZE=1                                                  # Test data size ,The 1 is 1GB.
PARALLEL=2                                                  # use <n> processes to generate data.
BUCKETNUM=8                                                 # HAWQ Table bucket number.
VSEG_RESOURCE_QUOTA=2gb                                     # HAWQ VSEG_RESOURCE_QUOTA default 256mb.
QUERY_STREAMS=1                                             # Query parallelism, currently maximum 20.
INSERT_STREAMS=8                                            # Insert parallelism (Load Data).
ISGENDATA=0                                                 # if ISGENDATA equal 0 then the test not generate data else need generate data. 

2 Run test

sh runall-gpload.sh

3 Only run generate data

sh gen_data.sh

4 Only run load data

set tpcds.config ISGENDATA=0

sh reload-gpload.sh 

5 Only run query

sh queries.sh

6 Delete generate data

sh deletefile.sh
